build:
  # if you have a Dockerfile, Cog will pick it up automatically.
  # Otherwise you can leave build empty and Cog will use default Python image.

predict:
  # no-op command, since you're just pushing a README
  command: echo "Readme pushed"
